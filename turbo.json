{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "apps/*/.next/**",
        "apps/*/dist/**",
        "apps/*/build/**"
      ]
    },
    "dev": {
      "cache": false
    },
    "lint": {},
    "test": {}
  }
}
